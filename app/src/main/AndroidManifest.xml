<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutAnil"></activity>
        <activity android:name=".AboutKishan" />
        <activity android:name=".AboutSahil" />
        <activity android:name=".AboutBhargav" />
        <activity android:name=".OrderDetailActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".MyOrdersActivity" />
        <activity android:name=".util.AboutActivity" />
        <activity android:name=".DashboardActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".BookDetailActivity" />
        <activity android:name=".BooksActivity" />
        <activity android:name=".CategoriesActivity" />
    </application>

</manifest>